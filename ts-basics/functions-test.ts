import { addNumbers, getName, joinNames, salutation } from './functions';

console.log(addNumbers(1, 2));
console.log(joinNames('a', 'b'));

console.log(getName({ first: 'Esmaeil', last: 'MIRZAEE' }));

console.log(salutation('Hi', 'Esmaeil', 'or', 'Sam'));
